<style type="text/css">
  .sidebar-nav {
    padding: 9px 0;
  }
</style>

<div class="well sidebar-nav">
  <ul class="nav nav-list">
    <% if !@project.videos.empty? %>
      <li><%= link_to "Videos", videos_project_path(@project.id) %></li>
    <% end %>
    <li class="nav-header">Posts</li>
    <% @project.posts.order("id desc").each do |post| %>
      <li><%= sidebar_link(post) %></li>
    <% end %>
    
    <% if @is_admin %>
      <br>
      <%= link_to "New Post", new_project_post_path(@project.id), :class => "btn btn-primary" %>
    <% end %>
  </ul>
</div>
